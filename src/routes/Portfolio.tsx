import { Typography } from "@mui/material";

const Portfolio = () => {
  return (
    <section>
      <Typography variant="h2">Portfolio</Typography>
      <Typography>I am a talented girly who can do many things!!</Typography>
    </section>
  );
};

export default Portfolio;
